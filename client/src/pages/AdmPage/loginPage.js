import React from 'react';

function loginPage() {
  return (
    <div
      style={{
        backgroundColor: '#000',
        color: '#fff',
        width: '100%',
        textAlign: 'center',
        padding: '10px 0',
        position: 'fixed',
        top: '120px',
        zIndex: 1030,
      }}
    >
      Painel Administrativo
    </div>
  );
}

export default loginPage;
